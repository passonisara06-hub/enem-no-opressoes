# Dependências de desenvolvimento e análise (notebook)
# Instale com: pip install -r requirements-dev.txt
# O dashboard em produção usa apenas requirements.txt (enxuto).
-r requirements.txt
openpyxl>=3.1
jupyter>=1.0
notebook>=7.0
